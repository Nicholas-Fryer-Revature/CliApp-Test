package cliapp

import scala.util.matching.Regex

class Cli {

  val commandPattern = Regex = "("

  def welcomeMessage(): Unit = {
    println("\nWelcome to CLI Test\n")
  }

  def printOptions(): Unit = {
    List(
      "Menu Options: "

    ).foreach(println)
  }

  def menu(): Unit = {
    var runLoop = true

    while(runLoop){
      Println("Operators: + - * / ^  Type X to Exit")

      

    }

  }

}